代码修改  1、 userLogin的insert，涉及到 add方法、add3rd等方法。
2、userlogin 的update，涉及到login以及login3rd相关的方法
3、get 方法涉及的userlogin返回，convertToUserDto私有方法内的，给注释掉。只有login相关的方法，返回这个对象信息。
4、随便找一个add方法，走一遍事物回滚的操作。
5、接口中@Deprecated掉addThirdUser。

科技运维部 - 王硕 2016-01-14 10:30:08 
导数据：
1、导入user以及userlogin对象的表。可以增量导入
2、密码测试一下，是否一致。导入user表后，对原密码进行移位操作
3、登录表信息，导入时为新增，如果用户有最后登录时间，就导入，没有就空着。userid必然导入。
4、注意咱们新增字段问题。新增的更新时间，导入时用创建时间。新增用户类型以及用户注册方式等新增用户字段，简单看一下是否正确的。

PC. individual.

